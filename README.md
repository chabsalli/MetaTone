# 📝 MetaTone (MVP)
**MetaTone — 성장 기록 기반 소프트스킬 트래커**는 사용자의 기록에서 **행동/배움**을 추출하고, 오늘 드러난 **소프트 스킬(1~3개)**을 근거 인용과 함께 정리해주는 Streamlit 앱입니다.  
또한 **Top 스킬 기준 2+2(연습 2개 + 질문 2개)** 루틴을 제안하고, 사용자가 메모를 누적 관리할 수 있게 돕습니다.

---

## ⭐ Core Features

1. **오늘의 기록 작성 (Single-box Reflection)**
   - 한 박스에 자유롭게 기록하되, `행동 → 감정 → 결과` 흐름을 권장
   - 날짜, 카테고리(선택/직접입력), 증거/자료 링크(선택) 지원

2. **상황분석: 행동 & 배움 추출 (No Summary)**
   - **actions(2~4)**, **learnings(1~2)**만 간결하게 정리

3. **소프트 스킬 분석 (1~3개) + 근거 인용**
   - 문제해결/의사소통/협업/리더십/자기관리·회복탄력성/학습역량 중 1~3개 제시
   - 각 스킬마다:
     - confidence(0~1)
     - **원문 근거 인용(evidence_quotes)**
     - 왜 해당 스킬인지(why_it_counts)
     - 스킬 개념(concept)

4. **성장 플랜 + 메모 누적 (Top Skill 2+2)**
   - Top 스킬 기준:
     - **연습/루틴 2개**
     - **다음 기록 질문 2개**
   - 문구 수정/메모 입력 가능, SQLite에 저장
   - 토글로 **다른 스킬**까지 메모 확장 가능

5. **기록 관리 & 탐색**
   - 기록 목록 조회, 본문 검색, 카테고리/스킬 필터
   - 기록별 재분석, 삭제 지원
   - TF-IDF 유사도 기반으로 **유사 기록 힌트(top-k)** 제공

6. **메모 보기 (날짜별 / 스킬별)**
   - 저장된 2+2 메모를 날짜 기준/스킬 기준으로 모아보기

---

## 📈 Expected Impact

- **회고 구조화 시간 단축**
  - 행동/배움 중심의 출력 형식으로 핵심만 빠르게 정리

- **소프트 스킬 인식의 일관성 향상**
  - 스킬별 **근거 인용 + 개념**을 함께 제공해 “왜 이 스킬인지”를 명확히 확인

- **성장 루틴의 지속성 강화**
  - Top 스킬 기준 **2+2 루틴**을 메모로 누적해 다음 기록에 바로 재사용 가능

---

## 🧠 Analysis Engine

- **무료(로컬) — 규칙/TF-IDF**
  - 키워드/패턴 기반으로 행동·배움/스킬을 보수적으로 추정
  - 유사 기록 힌트는 TF-IDF + cosine similarity 사용

- **LLM(OpenAI)**
  - OpenAI API Key가 있을 때 선택 가능
  - 출력은 **JSON 계약**을 따르며, 과장/단정 없이 근거 중심으로 생성  
  - LLM 실패 시 자동으로 로컬 분석으로 폴백

---

## 🗂 Data Storage

- **SQLite (metatone.db)**
  - `entries`: 기록 원문 + 분석 결과(JSON)
  - `skill_notes`: entry_id × skill_name 단위로 2+2 메모 저장(업서트)

---

## 🛠 Tech Stack

- **App**: Streamlit  
- **DB**: SQLite3  
- **Data**: Pandas  
- **NLP (local)**: scikit-learn (TF-IDF, cosine similarity)  
- **LLM (optional)**: OpenAI SDK (`openai>=1.0.0`)  
- **Deployment**: Streamlit Cloud

---

## ▶️ Run Locally

```bash
pip install -r requirements.txt
streamlit run app.py
